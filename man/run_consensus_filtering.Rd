% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_consensus_filtering.R
\name{run_consensus_filtering}
\alias{run_consensus_filtering}
\title{Run consensus filtering across pipelines}
\usage{
run_consensus_filtering(result)
}
\arguments{
\item{result}{Output from \code{\link[=run_strand_filtering]{run_strand_filtering()}}}
}
\value{
A list with components:
\itemize{
\item \code{merged}: Filtered consensus table (wide format, chr+pos+pipelines)
\item \code{unfiltered}: Same table before consensus filtering (for QC/plotting)
\item \code{thresholds}: Data.table with per-tool, per-tier (1M/2M/3M/4M) thresholds
\item \code{filtering_stats}: Data.table with overall before/after counts and retention \%
}
}
\description{
Convenience wrapper for the second stage of the pipeline. Takes output from
\code{\link[=run_strand_filtering]{run_strand_filtering()}}, adds consensus metrics (how many pipelines detected
each site), calculates per-tier filtering thresholds, and filters sites based
on cross-pipeline agreement.
}
\details{
Each underlying step (\code{\link[=calculate_consensus_stats]{calculate_consensus_stats()}},
\code{\link[=calculate_consensus_thresholds]{calculate_consensus_thresholds()}}, \code{\link[=filter_by_consensus]{filter_by_consensus()}}) remains
independently callable if you need to inspect or re-run an intermediate stage.
}
\examples{
\dontrun{
# Stage 1: Strand filtering
result_strand <- run_strand_filtering(
  pipelines = list(
    Bismark = bismark_dt,
    Bwameth = bwameth_dt,
    Biscuit = biscuit_dt,
    Encode = encode_dt
  )
)

# Stage 2: Consensus filtering
result_consensus <- run_consensus_filtering(result_strand)

# Access results
final_cpgs <- result_consensus$merged
stats <- result_consensus$filtering_stats
thresholds <- result_consensus$thresholds
}
}
